#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: the first number
 * @b: the second number
 *
 * Return: returns the result of the two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
