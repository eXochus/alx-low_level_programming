#include <stdio.h>

/**
 * main - defines the entry point of the code
 *
 * Return: nothing except 0
 */

int main(void)
{
	int c;

	for (c = 48; c <= 57; c++)
		putchar(c);
	putchar('\n');

	return (0);
}
