#include "main.h"

/**
 * _strpbrk - concatenates two stings
 * @s: string to be added to
 * @accept: strng to be added
 *
 * Return: dest.
 */

char *_strpbrk(char *s, char *accept)
{
	char *i;

	return (i);
}
